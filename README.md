# Card Management System API

## 📋 Описание проекта

**Card Management System API** — это Spring Boot веб-приложение для управления банковскими картами с системой авторизации и разграничением прав доступа на основе ролей пользователей.

## 🎯 Основные возможности

✅ **Регистрация и авторизация пользователей** (Spring Security + JWT Роли: ADMIN и USER)  
✅ **Управление банковскими картами** (CRUD операции)  
✅ **Ролевая модель доступа** (USER, ADMIN)  
✅ **Администрирование, управление**  
✅ **Создание и управление картами**  
✅ **Просмотр карт**  
✅ **Переводы между картами** и тд...  
✅ **Swagger документация API**  
✅ **Docker контейнеризация**

### Возможности

#### Администратор:
- Создает, блокирует, активирует, удаляет карты
- Управляет пользователями
- Видит все карты

#### Пользователь:
- Просматривает свои карты (поиск + пагинация)
- Запрашивает блокировку карты
- Делает переводы между своими картами
- Смотрит баланс

### 📡 API

- CRUD для карт
- Переводы между своими картами
- Фильтрация и постраничная выдача
- Валидация и сообщения об ошибках

## 🏗️ Технологический стек

### Backend:
- **Java 17+** (NetBeans)
- **Spring Boot 4.x**
- **Spring Security** (JWT аутентификация)
- **Spring Data JPA** (работа с БД)
- **Spring Web** (REST API)
- **Hibernate** (ORM)

### База данных:
- **PostgreSQL** (реляционная БД)

### Безопасность:
- **JWT** (JSON Web Tokens) для авторизации
- **BCryptPasswordEncoder** для хеширования паролей
- **Ролевая модель**: USER, ADMIN
- **CORS** настройки

### Документация и инструменты:
- **SpringDoc OpenAPI 3** (Swagger UI)
- **Docker & Docker Compose**
- **Maven** (сборка)
- Валидация DTO классами

## 🔐 Система безопасности

### Роли пользователей:

| Роль | Доступ |
|------|--------|
| **USER** | Управление своими картами, просмотр своего профиля |
| **ADMIN** | Полный доступ ко всем операциям, управление пользователями |

## 🚀 Развертывание Spring Boot Backend приложения

### Быстрый старт

#### 1. Сборка проекта

```bash
# Клонирование репозитория
git clone <ваш-репозиторий>
cd <директория-проекта>

# Сборка JAR файла
mvn clean package
```

#### 2. Запуск с Docker Compose

```bash
# Сборка и запуск всех сервисов
docker-compose up --build -d

# Или просто запуск
docker-compose up -d
```

#### 3. Проверка работоспособности

После запуска контейнера можно переходить на роут с API:

```
http://localhost:8081/swagger-ui/index.html - роут сваггера по умолчанию
```

**ГОТОВО!**

## 📚 Документация

Вся описывающая документация системы, технические схемы приложения и дамп с тестовыми данными для базы данных находятся в корне проекта, в папке `/docs`.